import { FosterRequestDirective } from './foster-request.directive';

describe('FosterRequestDirective', () => {
  it('should create an instance', () => {
    const directive = new FosterRequestDirective();
    expect(directive).toBeTruthy();
  });
});
